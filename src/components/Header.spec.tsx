// import { render } from '@testing-library/react'
// import { Header } from './Header'

describe('Test header', () => {
  it('test render', () => {
    // render(<Header />)
    expect(1).toBe(1)
  })
})
